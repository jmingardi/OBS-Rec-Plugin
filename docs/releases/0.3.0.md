- Generate and cache replay thumbnails asynchronously without blocking OBS startup or dock interaction.
- Display thumbnails directly in the replay table and recover automatically from missing or corrupt cached images.
- Preview the selected replay inside the OBS dock with play, pause, stop, seek, time, and missing-media handling.
- Route preview audio only through the configured OBS Monitoring Device so it never enters recordings or streams.
- Start every preview muted and reliably disconnect monitoring whenever the preview is muted, hidden, changed, or released.
- Expand the preview into a focus mode that hides surrounding controls and restores the full layout with the button or Escape.
- Resize the replay table and preview with a visible splitter and preserve the selected ratio across OBS restarts.
- Use a compact preview header, reclaim unused dock space, and align the Selected replay title with the Expand preview button.
- Open lifecycle diagnostics in a separate, closable window from the top toolbar while retaining logs between openings.
- Remove derived thumbnail cache files when sessions are cleared while preserving every replay and recording media file.
- Add FFmpeg-backed thumbnail generation coverage for scaling, cache reuse, corrupt-cache recovery, and missing media.
- Generate release descriptions from required versioned notes instead of Git commit and merge subjects.
- Replace the original conversation transcript with a concise product brief covering goals, behavior, architecture, and scope.
