- Add a dedicated editor export workflow for DaVinci Resolve timeline-marker EDL and Adobe Premiere-compatible Final Cut Pro 7 XML.
- Export one Resolve marker file per recording run and one Premiere XML project containing a marker sequence for each run.
- Collapse replay spans that cross split recording files into one run-global editor marker while preserving its complete duration.
- Support 23.976, 24, 25, 29.97 drop-frame, 30, 50, 59.94 drop-frame, 60, and 120 fps timelines with selectable zero-hour or one-hour starting timecode.
- Carry tags, ratings, notes, application metadata, replay paths, and mapping confidence into editor marker names and comments.
- Report Replay Buffer-only rows that were skipped because they have no recording timeline mapping.
- Write editor exports atomically and refuse unconfirmed overwrites when a multi-run Resolve export creates sibling EDL files.
